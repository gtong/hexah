plugins {
    id "com.moowork.node" version "0.10"
}

version '1.0'

node {
  version = '5.6.0'
  npmVersion = '3.6.0'
  download = true
}

task build(type: NpmTask) {
    args = ['run', 'build']
}

build.dependsOn(npm_install)
